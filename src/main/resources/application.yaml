spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vendas
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true

securiry:
  jwt:
    expiracao: 30
    chave-assinatura: QSBsZWJyZSDDqSBicmFuY2E=
